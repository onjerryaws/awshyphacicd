version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "installing dependencies..."
      - npm install
  build:
    commands:
      - echo "packaging the application..."
artifacts:
  files:
    - '**/*'
